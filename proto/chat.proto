syntax = "proto3";

package chat;

service Chat {
    rpc Poop (ChatRequest) returns (ChatResponse) {}
}


message ChatRequest {
    string name = 1;
}

message ChatResponse {
    string content = 1;
}
